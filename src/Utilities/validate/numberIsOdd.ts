/**
 * A function telling you if a number is odd.
 * Can in a pinch be used to tell if it's even.
 *
 * @param number to check if is odd
 * @returns
 */
export function numberIsOdd(number: number): boolean {
  return number % 2 === 1;
}
